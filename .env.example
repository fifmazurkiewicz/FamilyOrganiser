# ── Database ──
POSTGRES_USER=familyorg
POSTGRES_PASSWORD=changeme
POSTGRES_DB=familyorg
DATABASE_URL=postgresql+asyncpg://familyorg:changeme@localhost:5432/familyorg

# ── Redis ──
REDIS_URL=redis://localhost:6379/0

# ── Admin (seed) ──
ADMIN_EMAIL=admin@admin.com
ADMIN_PASSWORD=changeme

# ── Security ──
SECRET_KEY=changeme-in-production-use-long-random-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=30

# ── CORS ──
CORS_ORIGINS_STR=http://localhost:3000,http://localhost:80

# ── Frontend ──
VITE_API_URL=http://localhost:8000
