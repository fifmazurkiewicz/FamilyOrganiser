# Puste = requesty idą na to samo host:port (/api), Vite proxy → backend.
# Dzięki temu działa też z telefonu w LAN (http://IP-komputera:3000).
# Nie ustawiaj localhost — na telefonie localhost to sam telefon.
# VITE_API_URL=
